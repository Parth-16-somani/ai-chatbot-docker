services:
  chatbot:
    build: .
    container_name: ai-chatbot-container
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis-container
    ports:
      - "6379:6379"
